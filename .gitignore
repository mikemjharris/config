# We overrid global gitignore here as we want to commit our vimrc
!.vimrc

conf/new-nvim-setup/lazy-lock.json
conf/new-nvim-setup/lua/.luarc.json

**/.claude/settings.local.json

# Override global gitignore to allow .claude/ in this repo
!.claude/
# But still ignore personal files
.claude/settings.local.json

conf/tmux/plugins/

notes/

# Personal repo configuration
bin/repos.conf

# Personal tmux window configuration
local-exec/dev.windows

# Config for weekly repo summary
scripts/repos/
scripts/*.txt

CLAUDE.local.md
